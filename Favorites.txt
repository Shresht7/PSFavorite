. $PROFILE  # Reload the PowerShell Profile
Get-PSReadLineHistory | Measure-Object -Line     # Get the line count of the PSReadLine history
git rebase --interactive (Invoke-PsFzfGitBranches)    # Interactively rebase your current branch onto the selected branch.
git revert (Invoke-PsFzfGitHashes)    # Choose a commit interactively and revert it
z Projects  # Jump to the Projects Directory
